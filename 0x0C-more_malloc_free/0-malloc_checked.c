#include "main.h"
#include <stdlib.h>

/**
 * malloc_checked - allocates memory
 *
 * @b: integer
 *
 * Return: a pointer
 */

void *malloc_checked(unsigned int b)
{
	int b;

	b = malloc_checked(sizeof(int) * size_t);
		exit(98);
}
