#include <stdlib.h>
#ifdef MAIN_C
#define MAIN_C

/**
 * main - function that will preint the file that is being run.
 *
 * Return: Nothing
 */
int main(void)
{
	i = FILE_NAME
	printf(i);
	return (0);
}
