#ifndef FUNCTION_LIKE_MACRO_
#define FUNCTION_LIKE_MACRO_

#include <stdio.h>
#include <stdlib.h>

#define ABS(x)

int(main)
{
	int a;
	int b;

	x = abs(a) * 10;
	x = abs(b) * 10;
	printf("%d, %d\n", x);
	return (0);
}

#endif
